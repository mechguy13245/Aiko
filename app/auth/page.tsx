import { AuthPage } from "@/components/ui/auth-page";

export const metadata = {
  title: "Auth Showcase | Asme",
  description: "Sign In or Join Now to access the Asme Platform.",
};

export default function DemoOne() {
  return <AuthPage />;
}
